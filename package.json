{
  "name": "workflow-dispatch",
  "version": "2.0.0",
  "private": true,
  "description": "A customizable workflow dispatch UI builder for GitHub Actions",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently -n fe,be -c cyan,green \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
