workflows:
  test-dispatch.yml:
    title: "🚀 Deploy Application"
    description: "Deploy the application to a target environment with optional configuration"
    inputs:
      environment:
        label: "🌍 Target Environment"
      version:
        label: "📦 Version"
        placeholder: "e.g. v1.2.3 or latest"
        pattern: "^(latest|v\\d+\\.\\d+\\.\\d+.*)$"
        validationMessage: "Enter 'latest' or a semver version (e.g. v1.2.3)"
      dry_run:
        label: "🧪 Dry Run Mode"
      message:
        label: "📝 Deploy Notes"
        placeholder: "What changed in this deploy?"
      notify:
        label: "🔔 Send Notifications"
      tag:
        type: select
        label: "🏷️ Release Tag"
        optionsFrom:
          source: tags
      branch:
        type: select
        label: "🌿 Target Branch"
        optionsFrom:
          source: branches
    groups:
      - title: "Deployment"
        inputs: [environment, version, tag, branch]
      - title: "Options"
        inputs: [dry_run, notify, message]
